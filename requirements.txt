# 项目依赖文件 - 保守版本 (requirements-safe.txt)
# 此版本移除了非必需依赖，最大化安装成功率

# Web框架与服务器 (使用一组验证过的老版本)
fastapi==0.88.0
uvicorn[standard]==0.20.0
streamlit==1.22.0

# 核心数据处理
pandas==1.5.3
numpy==1.24.3
# 移除了 scikit-learn (初期非必需)

# 数据获取与API
requests==2.28.2
PyGithub==1.55

# 数据库
sqlalchemy==1.4.46
aiosqlite==0.17.0
# 移除了 alembic (初期非必需)

# 配置
python-dotenv==0.21.1
pydantic==1.10.7
# 移除了 pydantic-settings

# 可视化
plotly==5.13.1
# 移除了 matplotlib (初期非必需)

# 开发工具
black==22.12.0
flake8==6.0.0
pytest==7.2.2